Local Website To-Do List

1. Add a model/status panel
Show which model bundle is loaded, when it was trained, what dataset it used, and validation metrics per target.

2. Improve probability displays
Keep the bar charts, but add margin between the top two classes so users can see when a prediction is barely winning versus clearly separated.

3. Add uncertainty labels
Translate probabilities into plain language: Low confidence, Mixed signal, Strong signal.

4. Add example texts
Include a dropdown of sample Reddit-style comments, professional emails, casual texts, political comments, etc.

5. Add live comparison mode
Let users evaluate two texts side by side.

6. Add signal shift after edits
When the user reevaluates modified text, show arrows like Male +8%, Introverted -12%, Age under 25 +5%.

7. Add top cue terms
For TF-IDF models, show words or ngrams that most influenced each prediction.

8. Add privacy-risk framing
Alongside predictions, show a leakage meter per category: Gender signal: moderate, Age signal: weak, MBTI signal: strong.

9. Add calibration notes
For each target, show the model's validation accuracy or macro-F1 beside the result.

10. Remove or hide weak outputs
If full MBTI type accuracy is poor, hide it by default and emphasize the four binary dimensions. Keep advanced view for experimental outputs.

11. Add advanced/debug view
A collapsible panel with raw labels, probabilities, model names, and JSON output.

12. Add result history
Keep the last 5 evaluations in the browser session so users can compare how different text samples behave.

13. Add export/share
Allow downloading results as JSON or copying a small summary.

14. Add text stats
Show word count, character count, reading level, sentiment, punctuation density, etc.

15. Add target toggles
Let users choose which attributes to run or display: gender, age, MBTI dimensions, country, etc.

16. Add model comparison
Once the cloud model is back, allow switching between local prototype and PANDORA cloud-trained model to compare outputs.

17. Add explanation copy carefully
Short inline notes: This is a statistical association learned from training data, not a fact about the author.

18. Add loading/progress states
For larger model bundles, show Loading model, Evaluating, and response time.

19. Add input quality warnings
Warn if text is too short, mostly copied boilerplate, or too low-information for meaningful prediction.

20. Add a guided demo mode
A short sequence: evaluate sample text, edit it, compare signal shifts, inspect cue terms.
