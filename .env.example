# Flask bind settings
HOST=127.0.0.1
PORT=5000

# Public deployments should leave this false.
ENABLE_DEV_TOOLS=false

# If true, a missing trained model falls back to clearly labeled mock output.
# Set false in environments where a real model must be present.
TEXTTRAITS_ALLOW_DEMO=true
